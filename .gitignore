# Node — only present because the verify_*.mjs regression scripts use Playwright.
# Nothing in the deployed site depends on node_modules; the tools are self-contained HTML.
node_modules/
package-lock.json
package.json

# Playwright artifacts
test-results/
playwright-report/
.playwright/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Editor / scratch
.vscode/
.idea/
*.swp
*~

# Netlify local state
.netlify/

# Dev-only screenshot helper (local visual QA; not part of the site)
shot.mjs
_pwtest.mjs

# Build output (generated by build.mjs / demo-build.mjs; deployed by Netlify)
dist/
dist-demo/

# Guideline-watch run output. The report is regenerated on every run and posted
# to a GitHub issue; the fingerprint state lives on its own branch, pushed by
# .github/workflows/guideline-watch.yml — neither belongs in a clinical diff.
guideline-watch-report.md
guideline-watch-report.json
guideline-watch-state.json
