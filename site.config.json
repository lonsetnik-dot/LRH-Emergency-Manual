{
  "_comment": [
    "SITE IDENTITY — LRH edition (Littleton Regional Healthcare).",
    "Every {{SITE.key}} token in any .html file (and .webmanifest) is replaced with",
    "the value here at build time (build.mjs). The build FAILS if a token has no",
    "matching key, or if any un-substituted token survives into dist/ — identity can",
    "never silently half-apply.",
    "",
    "main is the LRH reference implementation — see SITES.md. build.mjs serves",
    "it at lrhemergencymanual.net; demo-build.mjs serves the same content as a",
    "ribboned showroom copy at demo.cairnready.org. There is no generic edition",
    "on main, deliberately: the demo shows a manual a real ED uses at the",
    "bedside. Identity lives here; clinical site-specific values stay in each",
    "tool's marked SITE CONFIG block, next to the logic that uses them and the",
    "citation for them. QR codes bake the domain below into their pixels —",
    "regenerate them (generate_qr_codes.py + the embedded matrices) if it changes."
  ],
  "title": "LRH Emergency Manual",
  "shortName": "LRH Manual",
  "abbrev": "LRH",
  "hospital": "Littleton Regional Healthcare",
  "eyebrow": "Littleton Regional Healthcare &middot; Emergency Department",
  "domain": "lrhemergencymanual.net",
  "editionNote": "Content is unverified; verify every value against local protocols and assays.",
  "footerLine": "Littleton Regional Healthcare Emergency Manual &mdash; personal, unofficial reference &middot; Beta",
  "_feedbackEmailNote": "Where the FEEDBACK button in every tool sends mail. Trunk-side this token replaced a hardcoded personal address that would have misrouted every fork's feedback; for LRH specifically it stays Lon's own address, since he is this edition's maintainer.",
  "feedbackEmail": "lon.setnik@gmail.com",
  "transferCenter": "DHMC Transfer Center",
  "transferPhone": "(877) 999-9870",

  "_transferDestinationsNote": [
    "RECEIVING HOSPITALS, one entry per destination, listed here ALPHABETICALLY by",
    "hospital name — which is also the order they are shown in, so a clinician",
    "scanning the list mid-code always finds a given hospital in the same place.",
    "",
    "transferCenter/transferPhone above stay as they are: they name the DEFAULT",
    "single destination (Dartmouth) for the cards that route to exactly one place,",
    "and duplicating Dartmouth here is deliberate rather than a stray copy.",
    "",
    "WHICH DESTINATIONS A CARD OFFERS is not decided here — it is decided in each",
    "tool's own SITE CONFIG block, because it is a clinical routing question, not",
    "an identity one. As agreed with Lon 2026-08-19: PE and STEMI offer all three;",
    "trauma offers Concord and Dartmouth only.",
    "",
    "Each destination carries three fields. `Label` is what is printed. `Phone` is",
    "what is shown to a human. `Tel` is the E.164 dial string behind the tap-to-",
    "call link — leave it EMPTY, not guessed, when the number is unknown: every",
    "renderer treats an undialable Tel as 'not configured' and prints the line as",
    "plain text instead of a link that dials nothing."
  ],
  "transferCmcLabel": "CMC (HCA New Hampshire) Transfer Line",
  "transferCmcPhone": "855-422-8200",
  "transferCmcTel": "+18554228200",
  "transferConcordLabel": "Concord Hospital Transfer Center",
  "transferConcordPhone": "800-992-9399",
  "transferConcordTel": "+18009929399",
  "transferDartmouthLabel": "Dartmouth-Hitchcock (DHMC) Transfer Center",
  "transferDartmouthPhone": "877-999-9870 (urgent/emergent)",
  "transferDartmouthTel": "+18779999870",

  "poisonControl": "1-800-222-1222"
}
